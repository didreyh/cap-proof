<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Tatak JPD – Cap Proof System</title>

  <script src="https://unpkg.com/html5-qrcode@2.3.10/html5-qrcode.min.js"></script>

  <script type="module">
    import { initializeApp } from "https://www.gstatic.com/firebasejs/10.13.0/firebase-app.js";
    import { getAuth, onAuthStateChanged, signOut }
      from "https://www.gstatic.com/firebasejs/10.13.0/firebase-auth.js";
    import {
      getFirestore, doc, getDoc, setDoc, updateDoc,
      serverTimestamp, collection, query, orderBy, limit, getDocs
    } from "https://www.gstatic.com/firebasejs/10.13.0/firebase-firestore.js";
    import { getStorage, ref, uploadBytes, getDownloadURL }
      from "https://www.gstatic.com/firebasejs/10.13.0/firebase-storage.js";

    // 🔑 FIREBASE CONFIG (SAME AS LOGIN)
    const firebaseConfig = {
      apiKey: "AIzaSyA9bjM96aB_HZ3Y7UXfSxbafi3lErGDB0c",
      authDomain: "cap-management---didreyh.firebaseapp.com",
      projectId: "cap-management---didreyh",
      storageBucket: "cap-management---didreyh.firebasestorage.app",
      messagingSenderId: "647273060104",
      appId: "1:647273060104:web:9419c250c8d06a4584febd"
    };

    const app = initializeApp(firebaseConfig);
    const auth = getAuth(app);

    // 🔐 PROTECT PAGE
    onAuthStateChanged(auth, (user) => {
      if (!user) {
        window.location.href = "login.html";
      }
    });

    window.logout = async () => {
      await signOut(auth);
      window.location.href = "login.html";
    };
  </script>

  <style>
    body{
      margin:0;
      font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Arial;
      background:#f6f6f7;
      color:#111;
    }
    .wrap{max-width:900px;margin:auto;padding:16px}
    .card{
      background:#fff;
      border-radius:16px;
      padding:16px;
      margin-bottom:12px;
      box-shadow:0 6px 18px rgba(0,0,0,.06);
    }
    h1{margin:0 0 6px;font-size:18px}
    .muted{font-size:12px;opacity:.7}
    button{
      padding:10px 14px;
      border-radius:12px;
      border:none;
      background:#111;
      color:#fff;
      font-weight:800;
      cursor:pointer;
    }
    button.secondary{
      background:#e9e9ec;
      color:#111;
      border:1px solid #ddd;
    }
  </style>
</head>

<body>
  <div class="wrap">
    <div class="card">
      <h1>🧢 Tatak JPD – Cap Proof System</h1>
      <div class="muted">
        Logged in • 
        <button class="secondary" onclick="logout()">Logout</button>
      </div>
    </div>

    <div class="card">
      <h2>✅ System Ready</h2>
      <p class="muted">
        Login successful 🎉<br>
        Next steps (we will add gradually):
        <br>• Scan AWB
        <br>• Add Kinder / Elem / SHS
        <br>• Upload photo proof (watermarked)
        <br>• Copy image + message
        <br>• Daily summary
      </p>
    </div>
  </div>
</body>
</html>