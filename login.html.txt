<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Tatak JPD – Login</title>

  <style>
    body{
      margin:0;
      font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Arial;
      background:#f6f6f7;
      display:flex;
      align-items:center;
      justify-content:center;
      height:100vh;
    }
    .card{
      background:#fff;
      padding:28px;
      border-radius:18px;
      width:100%;
      max-width:360px;
      box-shadow:0 10px 30px rgba(0,0,0,.08);
    }
    h1{margin:0 0 6px;font-size:20px;text-align:center}
    p{margin:0 0 20px;text-align:center;font-size:13px;opacity:.7}
    label{font-size:12px;margin-bottom:6px;display:block}
    input{
      width:100%;
      padding:12px;
      border-radius:12px;
      border:1px solid #ddd;
      margin-bottom:14px;
      font-size:15px;
      box-sizing:border-box;
    }
    button{
      width:100%;
      padding:12px;
      border-radius:12px;
      border:none;
      background:#111;
      color:#fff;
      font-weight:800;
      font-size:15px;
      cursor:pointer;
    }
    #msg{
      margin-top:12px;
      text-align:center;
      font-size:13px;
    }
    .muted{
      opacity:.7;
      font-size:12px;
      text-align:center;
      margin-top:10px;
    }
  </style>

  <script type="module">
    import { initializeApp } from "https://www.gstatic.com/firebasejs/10.13.0/firebase-app.js";
    import { getAuth, signInWithEmailAndPassword, onAuthStateChanged }
      from "https://www.gstatic.com/firebasejs/10.13.0/firebase-auth.js";

    // 🔑 FIREBASE CONFIG (PASTE NA – OK NA ITO)
    const firebaseConfig = {
      apiKey: "AIzaSyA9bjM96aB_HZ3Y7UXfSxbafi3lErGDB0c",
      authDomain: "cap-management---didreyh.firebaseapp.com",
      projectId: "cap-management---didreyh",
      storageBucket: "cap-management---didreyh.firebasestorage.app",
      messagingSenderId: "647273060104",
      appId: "1:647273060104:web:9419c250c8d06a4584febd"
    };

    const app = initializeApp(firebaseConfig);
    const auth = getAuth(app);

    // If already logged in, go to main app
    onAuthStateChanged(auth, (user) => {
      if (user) {
        window.location.href = "index.html";
      }
    });

    window.login = async () => {
      const email = document.getElementById("email").value.trim();
      const password = document.getElementById("password").value;
      const msg = document.getElementById("msg");

      msg.style.color = "#111";
      msg.textContent = "Logging in…";

      try {
        await signInWithEmailAndPassword(auth, email, password);
        window.location.href = "index.html";
      } catch (e) {
        msg.style.color = "#b00020";
        msg.textContent = "❌ Invalid email or password";
      }
    };
  </script>
</head>

<body>
  <div class="card">
    <h1>🔐 Tatak JPD</h1>
    <p>Cap Packing & AWB Proof System</p>

    <label>Email</label>
    <input id="email" type="email" placeholder="you@email.com"/>

    <label>Password</label>
    <input id="password" type="password" placeholder="••••••••"/>

    <button onclick="login()">Login</button>
    <div id="msg"></div>

    <div class="muted">Private system — login required</div>
  </div>
</body>
</html>